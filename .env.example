# Dual-scorer shadow track (never commit a real .env — it is gitignored)
XAI_API_KEY=

# Optional overrides (defaults in shadow/config.py)
# SHADOW_GROK_MODEL=grok-4
# XAI_BASE_URL=https://api.x.ai/v1
# NERVA_KERNEL_REF=v11.1-stable
